"""Camera + Claude Vision smoke test (Phase 3 implementation).

Pulls one frame from CAMERA_URL, sends it to Claude Vision with a generic
description prompt, and prints the response. Verifies the end-to-end
pipeline before the modes layer is built.
"""

from __future__ import annotations

import sys


def main() -> int:
    """Entry point."""
    print("Phase 3 stub: vision smoke test not yet implemented.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
